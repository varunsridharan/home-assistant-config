- sensor:
    state: "{{ as_timestamp(state_attr('sun.sun','next_rising')) | timestamp_custom('%H:%M')  }}"
    name: "Sun Rise"
    unique_id: 'sunrise_time'
    attributes:
      1min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) - ( 60 * 1 ) ) | timestamp_custom('%H:%M')  }}"
      5min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) - ( 60 * 5 ) ) | timestamp_custom('%H:%M')  }}"
      10min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) - ( 60 * 10 ) ) | timestamp_custom('%H:%M')  }}"
      15min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) - ( 60 * 15 ) ) | timestamp_custom('%H:%M')  }}"
      20min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) - ( 60 * 20 ) ) | timestamp_custom('%H:%M')  }}"
      25min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) - ( 60 * 25 ) ) | timestamp_custom('%H:%M')  }}"
      30min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) - ( 60 * 30 ) ) | timestamp_custom('%H:%M')  }}"
      35min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) - ( 60 * 35 ) ) | timestamp_custom('%H:%M')  }}"
      40min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) - ( 60 * 40 ) ) | timestamp_custom('%H:%M')  }}"
      45min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) - ( 60 * 45 ) ) | timestamp_custom('%H:%M')  }}"
      50min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) - ( 60 * 50 ) ) | timestamp_custom('%H:%M')  }}"
      55min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) - ( 60 * 55 ) ) | timestamp_custom('%H:%M')  }}"
      60min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) - ( 60 * 60 ) ) | timestamp_custom('%H:%M')  }}"

      1min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) + ( 60 * 1 ) ) | timestamp_custom('%H:%M')  }}"
      5min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) + ( 60 * 5 ) ) | timestamp_custom('%H:%M')  }}"
      10min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) + ( 60 * 10 ) ) | timestamp_custom('%H:%M')  }}"
      15min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) + ( 60 * 15 ) ) | timestamp_custom('%H:%M')  }}"
      20min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) + ( 60 * 20 ) ) | timestamp_custom('%H:%M')  }}"
      25min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) + ( 60 * 25 ) ) | timestamp_custom('%H:%M')  }}"
      30min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) + ( 60 * 30 ) ) | timestamp_custom('%H:%M')  }}"
      35min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) + ( 60 * 35 ) ) | timestamp_custom('%H:%M')  }}"
      40min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) + ( 60 * 40 ) ) | timestamp_custom('%H:%M')  }}"
      45min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) + ( 60 * 45 ) ) | timestamp_custom('%H:%M')  }}"
      50min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) + ( 60 * 50 ) ) | timestamp_custom('%H:%M')  }}"
      55min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) + ( 60 * 55 ) ) | timestamp_custom('%H:%M')  }}"
      60min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_rising')) + ( 60 * 60 ) ) | timestamp_custom('%H:%M')  }}"

- sensor:
    state: "{{ as_timestamp(state_attr('sun.sun','next_setting')) | timestamp_custom('%H:%M')  }}"
    name: "Sun Set"
    unique_id: 'sunset_time'
    attributes:
      1min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) - ( 60 * 1 ) ) | timestamp_custom('%H:%M')  }}"
      5min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) - ( 60 * 5 ) ) | timestamp_custom('%H:%M')  }}"
      10min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) - ( 60 * 10 ) ) | timestamp_custom('%H:%M')  }}"
      15min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) - ( 60 * 15 ) ) | timestamp_custom('%H:%M')  }}"
      20min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) - ( 60 * 20 ) ) | timestamp_custom('%H:%M')  }}"
      25min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) - ( 60 * 25 ) ) | timestamp_custom('%H:%M')  }}"
      30min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) - ( 60 * 30 ) ) | timestamp_custom('%H:%M')  }}"
      35min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) - ( 60 * 35 ) ) | timestamp_custom('%H:%M')  }}"
      40min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) - ( 60 * 40 ) ) | timestamp_custom('%H:%M')  }}"
      45min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) - ( 60 * 45 ) ) | timestamp_custom('%H:%M')  }}"
      50min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) - ( 60 * 50 ) ) | timestamp_custom('%H:%M')  }}"
      55min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) - ( 60 * 55 ) ) | timestamp_custom('%H:%M')  }}"
      60min_before : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) - ( 60 * 60 ) ) | timestamp_custom('%H:%M')  }}"

      1min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) + ( 60 * 1 ) ) | timestamp_custom('%H:%M')  }}"
      5min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) + ( 60 * 5 ) ) | timestamp_custom('%H:%M')  }}"
      10min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) + ( 60 * 10 ) ) | timestamp_custom('%H:%M')  }}"
      15min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) + ( 60 * 15 ) ) | timestamp_custom('%H:%M')  }}"
      20min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) + ( 60 * 20 ) ) | timestamp_custom('%H:%M')  }}"
      25min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) + ( 60 * 25 ) ) | timestamp_custom('%H:%M')  }}"
      30min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) + ( 60 * 30 ) ) | timestamp_custom('%H:%M')  }}"
      35min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) + ( 60 * 35 ) ) | timestamp_custom('%H:%M')  }}"
      40min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) + ( 60 * 40 ) ) | timestamp_custom('%H:%M')  }}"
      45min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) + ( 60 * 45 ) ) | timestamp_custom('%H:%M')  }}"
      50min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) + ( 60 * 50 ) ) | timestamp_custom('%H:%M')  }}"
      55min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) + ( 60 * 55 ) ) | timestamp_custom('%H:%M')  }}"
      60min_after : "{{ ( as_timestamp(state_attr('sun.sun','next_setting')) + ( 60 * 60 ) ) | timestamp_custom('%H:%M')  }}"